# values.yaml
replicaCount: 2

image:
  repository: nginx
  tag: latest
  pullPolicy: IfNotPresent

service:
  type: LoadBalancer
  port: 80

persistence:
  storageClass: ""
  accessMode: ReadWriteOnce
  size: 1Gi
